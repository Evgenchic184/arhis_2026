# C4 Architecture

## Context

```mermaid
flowchart LR
  user["User"]
  moderator["Moderator"]
  admin["Admin"]
  frontend["Svelte Frontend"]
  api["FastAPI Backend"]
  postgres["PostgreSQL"]
  redis["Redis"]
  kafka["Kafka"]
  kafka_ui["Kafka UI"]
  ml_worker["ML inference worker"]
  retraining_monitor["Retraining monitor"]
  minio["MinIO"]
  model_registry_ctx["Model registry\n(Postgres + MinIO)"]
  prometheus["Prometheus"]
  grafana["Grafana"]
  parquet["Parquet / S3 Sink"]

  user --> frontend
  moderator --> frontend
  admin --> frontend
  frontend --> api
  api --> postgres
  api --> redis
  api --> kafka
  kafka_ui --> kafka
  ml_worker --> kafka
  ml_worker --> postgres
  ml_worker --> model_registry_ctx
  retraining_monitor --> minio
  retraining_monitor --> postgres
  retraining_monitor --> model_registry_ctx
  model_registry_ctx --> postgres
  kafka --> parquet
  parquet --> minio
  model_registry_ctx --> minio
  api --> prometheus
  prometheus --> grafana
```

## Containers

```mermaid
flowchart LR
  subgraph browser["Client"]
    frontend["frontend/\nSvelte + Vite + Nginx"]
  end

  subgraph backend["Backend"]
    api["src/app\nFastAPI app"]
    outbox_relay["src/app/workers/outbox_relay.py"]
    ml_worker["src/app/workers/ml_inference_worker.py"]
    parquet_sink["src/app/workers/parquet_sink.py"]
    retraining_monitor["src/app/workers/retraining_monitor.py"]
  end

  subgraph storage["Data layer"]
    postgres["PostgreSQL\nsource of truth + outbox"]
    redis["Redis\nonline feature store + moderation queue fallback"]
    kafka["Kafka\ndomain event stream"]
    s3["MinIO / S3\noffline history"]
    model_registry_store["Model registry\nversions + artifacts"]
  end

  subgraph observability["Observability"]
    prometheus["Prometheus"]
    grafana["Grafana"]
    kafka_ui["Kafka UI"]
  end

  frontend --> api
  api --> postgres
  api --> redis
  api --> kafka
  outbox_relay --> kafka
  ml_worker --> kafka
  ml_worker --> postgres
  kafka_ui --> kafka
  parquet_sink --> kafka
  ml_worker --> model_registry_store
  retraining_monitor --> model_registry_store
  parquet_sink --> s3
  retraining_monitor --> s3
  model_registry_store --> postgres
  model_registry_store --> s3
  api --> prometheus
  prometheus --> grafana
```

## Backend components

```mermaid
flowchart TB
  subgraph app["src/app"]
    main["main.py\nFastAPI app + middleware"]
    auth["api/v1/routes/auth.py"]
    posts["api/v1/routes/posts.py"]
    comments["api/v1/routes/comments.py"]
    reports["api/v1/routes/reports.py"]
    users["api/v1/routes/users.py"]
    features["api/v1/routes/feature_config.py"]
    core_db["core/database.py"]
    core_events["core/events.py"]
    core_kafka["core/kafka.py"]
    core_queue["core/queue.py"]
    core_metrics["core/monitoring.py"]
    core_logging["core/logging.py"]
    models["models/*"]
    workers["workers/*"]
    ml_worker["workers/ml_inference_worker.py"]
  end

  main --> auth
  main --> posts
  main --> comments
  main --> reports
  main --> users
  main --> features
  auth --> core_db
  posts --> core_db
  comments --> core_db
  reports --> core_db
  users --> core_db
  features --> core_db
  posts --> core_events
  comments --> core_events
  reports --> core_events
  users --> core_events
  auth --> core_events
  core_events --> models
  workers --> core_db
  workers --> core_kafka
  workers --> core_queue
  ml_worker --> core_db
  ml_worker --> core_kafka
  ml_worker --> core_queue
  ml_worker --> core_events
  ml_worker --> models
  core_metrics --> core_logging
  reports --> core_queue
  reports --> core_events
```

## Key responsibilities

- `PostgreSQL` stores users, posts, comments, reports, and the domain-event outbox.
- `Redis` stores runtime feature config, online feature state, and queue fallback data.
- `Kafka` is the event backbone for domain events.
- `Kafka` also carries moderation ML requests for the inference worker.
- `Retraining monitor` reads parquet archives, computes drift metrics, and launches retraining jobs.
- `Kafka UI` is the local inspection surface for topics and consumer groups.
- `MinIO / S3` is the offline sink for analytics and training data.
- `Model registry` stores versioned model metadata in Postgres and artifacts in MinIO / S3.
