# Постановка задачи: Модерация пользовательского контента

## 🎯 Бизнес-задача (финальная версия)

**Система автоматической модерации комментариев для Reddit-подобной платформы**

---

## 📋 Что классифицируем:

### Основная задача:
```
Комментарий → [Разрешить / Заблокировать / На модерацию]
```

### Типы нарушающего контента для детекции:

| Тип | Описание | Пример | Действие |
|-----|----------|--------|----------|
| **Токсичность** | Оскорбления, угрозы, буллинг | "You're stupid, kill yourself" | Блокировка |
| **Hate Speech** | Ненависть по группе (раса, религия, ориентация) | "All [group] are inferior" | Блокировка + эскалация |
| **Спам** | Рекламные вставки, повторы, боты | "Check out my link!!!" | Блокировка |
| **Оффтопик** | Комментарий не по теме поста | (требуется детекция релевантности) | Скрыть / На модерацию |

---

## 📊 Доступные датасеты для этих задач:

### 1. Cyberbullying Classification (47K)

**Что детекти:** Токсичность + Hate Speech

**Классы:**
```
not_cyberbullying      → 0 (норма)
age                    → 1 (age harassment)
ethnicity              → 1 (hate speech)
gender                 → 1 (sexism)
religion               → 1 (religious harassment)
sexual_orientation     → 1 (LGBTQ+ harassment)
other_cyberbullying    → 1 (general toxicity)
```

**Маппинг для бинарной классификации:**
```python
is_toxic = 1 if cyberbullying_type != 'not_cyberbullying' else 0
```

**Баланс:** 5:1 в пользу токсичных (39K toxic, 8K not toxic)

---

### 2. Reddit Sentiment Analysis (~100K)

**Что детекти:** **Не используется для модерации напрямую**

**Почему:** Sentiment (Positive/Negative/Neutral) — это про **эмоции**, а не про нарушения.

**Возможное использование:**
- ❌ Не для блокировок (грустный комментарий ≠ нарушение)
- ⚠️ Можно как вспомогательный сигнал (агрессивный негатив → возможная токсичность)

**Решение:** **Не используем для v1** — нет разметки нарушений

---

### 3. Что нужно для детекции спама и оффтопика?

#### Спам:
**Проблема:** В доступных датасетах нет разметки спама.

**Решения:**
1. **Эвристики** (не требует датасета):
   - Наличие URL в комментарии
   - Длина < 5 символов
   - Повторы одного текста
   - CAPS LOCK > 80%
   - Возраст аккаунта < 1 дня

2. **Отдельный датасет:**
   - [SMS Spam Collection](https://www.kaggle.com/datasets/uciml/sms-spam-collection-dataset) — SMS, не комментарии
   - Искать датасеты с spam/ham разметкой для соцсетей

#### Оффтопик (релевантность):
**Проблема:** Нет датасетов с разметкой "релевантно/не релевантно посту".

**Решения:**
1. **Эвристики:**
   - Similarity между текстом поста и комментария (cosine similarity TF-IDF / embeddings)
   - Наличие ключевых слов из поста в комментарии
   - Длина комментария (слишком короткие = возможный оффтопик)

2. **ML модель:**
   - Нужен датасет с парами (пост, комментарий) + label (relevant/irrelevant)
   - Можно собрать свои данные (скрапинг Reddit + ручная разметка)

3. **NLI модель (Natural Language Inference):**
   - Предобученные модели (RoBERTa-MNLI) могут детекти entailment/contradiction
   - Но не факт, что хорошо работает для релевантности

---

## 🎯 Финальная рекомендация для v1

### Задача: **Toxic Content Detection**

**Датасет:** Cyberbullying Classification

**Классификация:**
```
Комментарий → [Токсичный / Не токсичный]
```

**Бизнес-логика:**
```
Токсичный → Блокировка / Флаг модератору
Не токсичный → Разрешить
```

**Почему:**
- ✅ Один датасет с чёткой разметкой нарушений
- ✅ Детектирует токсичность + hate speech
- ✅ Простая бинарная задача
- ✅ Можно реализовать и защитить в сроки

---

## 📋 План по детекции других типов нарушений:

### v1 (Checkpoint 1-2): Toxic Detection
```
Модель: Logistic Regression / Random Forest / BERT
Датасет: Cyberbullying Classification
Фичи: TF-IDF / Text embeddings
```

### v2 (Checkpoint 3-4): + Spam Detection
```
Подход: Эвристики + возможно ML
Фичи: URL presence, text length, repetition rate, account age
```

### v3 (расширение): + Relevance Detection
```
Подход: Similarity scoring (TF-IDF cosine / embeddings)
Фичи: Similarity(post_text, comment_text)
```

---

## 🏗️ Архитектура системы (финальная)

```
                    ┌──────────────────────┐
                    │   Комментарий        │
                    └──────────┬───────────┘
                               │
         ┌─────────────────────┼─────────────────────┐
         │                     │                     │
         ▼                     ▼                     ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   Toxic Detector│  │   Spam Detector │  │  Relevance      │
│   (ML модель)   │  │   (эвристики)   │  │  (similarity)   │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         │                    │                     │
         └────────────────────┼─────────────────────┘
                              │
                              ▼
                   ┌──────────────────────┐
                   │   Decision Engine    │
                   └──────────┬───────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
   Блокировка            Флаг модератору      Разрешить
   (toxic/spam)          (пограничные)
```

### Decision Engine правила:

```python
def moderate_comment(comment, post_text=None):
    toxic_score = toxic_model.predict([comment])[0]
    spam_score = calculate_spam_score(comment)  # эвристики
    relevance_score = calculate_relevance(comment, post_text)  # similarity
    
    # Токсичный → блокировка
    if toxic_score > 0.7:
        return "BLOCK", "Toxic content"
    
    # Спам → блокировка
    if spam_score > 0.8:
        return "BLOCK", "Spam detected"
    
    # Оффтопик → на модерацию (если есть пост)
    if post_text and relevance_score < 0.3:
        return "REVIEW", "Possible off-topic"
    
    # Пограничные случаи → флаг
    if toxic_score > 0.4:  # ниже порога, но подозрительно
        return "FLAG", "Borderline toxic"
    
    # Норма
    return "PASS", "No violations"
```

---

## 📊 Метрики качества для v1 (Toxic Detection)

| Метрика | Target | Обоснование |
|---------|--------|-------------|
| **Precision** | > 0.85 | Минимизировать ложные блокировки (пользователи жалуются) |
| **Recall** | > 0.75 | Ловить большинство нарушений |
| **F1-Score** | > 0.80 | Баланс |
| **ROC-AUC** | > 0.85 | Общее качество классификации |

**Важно:** Precision важнее Recall — лучше пропустить токсичный, чем заблокировать нормальный комментарий.

---

## 📝 Следующие шаги дляCheckpoint 1:

1. ✅ **Выбрать задачу:** Toxic Content Detection
2. ✅ **Выбрать датасет:** Cyberbullying Classification
3. ✅ **Определить бизнес-логику:** Блокировка/флаг для токсичных
4. ⏳ **Скачать датасет** с Kaggle
5. ⏳ **Провести EDA:**
   - Распределение классов (is_toxic после маппинга)
   - Длины текстов
   - Топ слов для toxic vs not_toxic
   - Пропуски, дубликаты
6. ⏳ **Определить baseline:** LogReg + TF-IDF
7. ⏳ **Определить метрики:** Precision, Recall, F1

---

## ❓ Вопросы на согласование:

1. **Согласен с задачей Toxic Detection для v1?**
2. **Нужно ли добавлять Spam Detection через эвристики сразу или отложить на v2?**
3. **Нужна ли детекция оффтопика (релевантности) в scope проекта?**

---

**Резюме:** Забываем про поддержку. Фокус на **модерации**: токсичность → блокировка/флаг. Используем Cyberbullying dataset для v1.
